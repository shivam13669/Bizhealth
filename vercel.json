{
  "buildCommand": "pnpm build:client",
  "installCommand": "pnpm install",
  "framework": null,
  "outputDirectory": "dist/spa",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api"
    },
    {
      "source": "/:path*",
      "destination": "/"
    }
  ]
}
